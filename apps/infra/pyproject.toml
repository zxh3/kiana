[project]
name = "infra"
version = "0.1.0"
description = "AWS infrastructure managed with Pulumi"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pulumi>=3.0.0,<4.0.0",
    "pulumi-aws>=7.0.0,<8.0.0",
]

[tool.uv]
package = false
