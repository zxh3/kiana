[project]
name = "dummy-server"
version = "0.1.0"
description = "A tiny FastAPI service for learning Kubernetes deployments."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.116,<1",
    "uvicorn>=0.35,<1",
]

[tool.uv]
package = false
