/**
 * Signing in with Google, as the browser and the Worker
 * (`src/server/auth.ts`) both see it.
 */

/** Where Better Auth's routes are: signing in, the callback, the session. */
export const AUTH_PATH = "/api/auth";
